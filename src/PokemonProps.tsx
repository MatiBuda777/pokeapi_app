export type PokemonProps = {
    abilities: any[],
    height: number,
    id: number,
    name: string,
    sprites?: { front_default?: string };
    types: any[],
    weight: number,
    url: string,
}